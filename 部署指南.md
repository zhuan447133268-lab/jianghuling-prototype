# 江湖令原型 - 访问方式

## 方法一：直接在浏览器打开（立即可用）

1. 找到文件：`D:\opencode\江湖令原型\index.html`
2. 双击 `index.html` 文件，即可在浏览器中打开
3. 或者复制以下路径到浏览器地址栏：
   ```
   file:///D:/opencode/江湖令原型/index.html
   ```

## 方法二：本地局域网访问（可分享给同网络的人）

在命令行中执行以下命令启动本地服务器：

**Windows (PowerShell):**
```powershell
cd D:\opencode\江湖令原型
python -m http.server 8888
```

**Windows (CMD):**
```cmd
cd D:\opencode\江湖令原型
python -m http.server 8888
```

然后在浏览器中访问：
```
http://192.168.50.23:8888
```

> 注意：此链接只能在同一局域网内访问，不能分享给外网的人

## 方法三：部署到 GitHub Pages（推荐，可在线分享）

### 步骤1：创建 GitHub 仓库

1. 访问：https://github.com/new
2. 仓库名称：输入 `jianghuling-prototype`（或其他名称）
3. 选择 **Public**（公开）
4. 不要初始化 README、.gitignore 或 license
5. 点击 **Create repository**

### 步骤2：推送代码

在命令行中执行以下命令（替换 `YOUR_USERNAME` 为你的 GitHub 用户名）：

```bash
cd D:\opencode\江湖令原型
git remote add origin https://github.com/YOUR_USERNAME/jianghuling-prototype.git
git branch -M main
git push -u origin main
```

### 步骤3：启用 GitHub Pages

1. 访问你的仓库：https://github.com/YOUR_USERNAME/jianghuling-prototype
2. 点击仓库顶部的 **Settings** 按钮
3. 在左侧菜单中找到 **Pages**
4. 在 **Build and deployment** > **Source** 中，选择 **Deploy from a branch**
5. 在 **Branch** 中选择 **main**（或 master），文件夹选择 **/(root)**
6. 点击 **Save**

### 步骤4：访问部署的网站

等待 1-2 分钟后，访问：
```
https://YOUR_USERNAME.github.io/jianghuling-prototype/
```

> 将这个链接分享给任何人都可以访问！

## 方法四：使用 Netlify/Vercel（一键部署）

### 使用 Netlify

1. 访问：https://app.netlify.com/drop
2. 将 `D:\opencode\江湖令原型` 文件夹拖拽到浏览器中
3. 等待部署完成，Netlify 会给你一个在线链接（如 `https://xxx.netlify.app`）

### 使用 Vercel

1. 访问：https://vercel.com/new
2. 选择 **Import Git Repository**
3. 导入你刚创建的 GitHub 仓库
4. 点击 **Deploy**
5. Vercel 会给你一个在线链接（如 `https://xxx.vercel.app`）

---

## 推荐方案

**最快方案：** 方法一（直接打开文件）

**可分享方案：** 方法三（GitHub Pages）或 方法四（Netlify 拖拽部署）

**专业方案：** 方法四（Vercel，支持自定义域名）
